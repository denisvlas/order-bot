export interface Product {
    price: number
    imageUrl: string
    name: string
    description: string
    id: string
    stock: number
}
export interface CartItem {
    price: number
    imageUrl: string
    name: string
    description: string
    id: string
    stock: number
    quantity: number
}
